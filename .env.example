# Welcome to the .env.example file!

# This file serves as a template for your environment variables.
# When setting up your local development or production environment,
# create a .env or .env.local file and define these variables.

# Local Development Environment Variables:
# ------------------------------

# For local development, you can use a local API URL (e.g., http://localhost:8000)
REACT_APP_API_URL=http://localhost:8000

# Set this to your local API key (if needed)
REACT_APP_API_KEY=your-local-api-key

# Note:
# - Prefix your environment variables with REACT_APP_ for proper recognition.
# - Never commit your .env or .env.local files with actual keys or sensitive information.
# - Keep this .env.example file up-to-date to help developers configure their environment.
# - Remove the '#' symbol and set values in your .env or .env.local files.
